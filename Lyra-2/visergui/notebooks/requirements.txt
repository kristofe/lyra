# pip requirements for the two standalone notebooks in this folder:
#   splat_init_and_train.ipynb   (video/frames -> DA3 -> gaussian init -> training)
#   splat_segmentation.ipynb     (click -> SAM2 masks -> splat selection -> object/background PLYs)
#
# Python 3.10. Install torch FIRST from the cu128 index, then the rest:
#   pip install torch==2.10.0+cu128 torchvision==0.25.0 --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements.txt
#
# Two packages are NOT on PyPI / need extra steps:
#   depth_anything_3  (init/train notebook only) — editable install from the repo submodule:
#       git submodule update --init --recursive lyra_2/_src/inference/depth_anything_3
#       pip install --no-deps -e ./lyra_2/_src/inference/depth_anything_3   # (from Lyra-2/; pins numpy<2, hence --no-deps)
#   SAM 2.1 checkpoint (segmentation notebook only) — ~900 MB, into Lyra-2/vendor/InstaInpaint/checkpoints/:
#       curl -L -o vendor/InstaInpaint/checkpoints/sam2.1_hiera_large.pt https://dl.fbaipublicfiles.com/segment_anything_2/092824/sam2.1_hiera_large.pt
#
# gsplat compiles its CUDA kernels at first use (needs nvcc + a host C++ compiler);
# on Windows run via run_train_smoke.cmd / run_seg_smoke.cmd which set that env up.

# -- shared core --
numpy==2.2.6
opencv-python==4.11.0.86
plyfile==1.1.3
pillow
tqdm
matplotlib

# -- notebook execution / harness --
ipykernel
nbformat
nbconvert
papermill

# -- init/train notebook: DA3 runtime deps (installed here because DA3 itself is --no-deps) --
addict==2.4.0
omegaconf==2.3.0
einops==0.8.2
safetensors
huggingface_hub[hf_xet]
imageio==2.37.3
pycolmap==4.0.4
trimesh==4.12.2
evo==1.36.4
scipy==1.15.3

# -- training / rendering --
gsplat==1.5.3
lpips==0.1.4

# -- segmentation notebook --
sam2==1.1.0
